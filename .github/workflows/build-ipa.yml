name: Build Off-Day IPA (TrollStore专用)
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 清理 Xcode 缓存
        run: |
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          rm -rf ~/Library/Caches/com.apple.dt.Xcode
          rm -rf ~/Library/Caches/com.apple.dt.XCODe
          echo "✅ 清理完成"

      - name: 检查项目信息（排查用）
        run: |
          echo "=== 项目文件列表 ==="
          ls -la
          echo "=== 找到的Xcode项目 ==="
          find . -name "*.xcodeproj"
          echo "=== 项目可用Scheme列表 ==="
          xcodebuild -list -project "Off Day.xcodeproj"

      - name: 无签名编译打包（巨魔专用）
        run: |
          PROJECT="Off Day.xcodeproj"
          SCHEME="Off Day"

          xcodebuild clean archive \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "Off-Day.xcarchive" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE="" \
            PRODUCT_BUNDLE_IDENTIFIER=com.offday.trollbuild \
            SKIP_INSTALL=NO \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES

          APP_PATH="Off-Day.xcarchive/Products/Applications/Off Day.app"
          if [ ! -d "$APP_PATH" ]; then
            echo "❌ 编译失败，请检查上面的详细错误日志"
            exit 1
          fi

          rm -rf Payload Off-Day.ipa
          mkdir Payload
          cp -R "$APP_PATH" Payload/
          zip -r Off-Day.ipa Payload/
          rm -rf Payload

          echo "✅ IPA打包成功！文件大小：$(ls -lh Off-Day.ipa | awk '{print $5}')"

      - name: 上传IPA成品
        uses: actions/upload-artifact@v4
        with:
          name: Off-Day-IPA
          path: Off-Day.ipa
          retention-days: 7
